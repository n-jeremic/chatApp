doctype html
head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    title So Much Fun | Error
h2= `${error.statusCode} ${error.status}`
h1= `${error.message}`
